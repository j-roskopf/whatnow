# Server-side API keys (Vercel project settings → Environment Variables).
# Optional locally: copy to .env for `npm run generate-data`.
STEAMGRIDDB_KEY=
IGDB_CLIENT_ID=
IGDB_CLIENT_SECRET=
OPENCRITIC_KEY=
